// /** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig


/** @type {import('next').NextConfig} */
// const nextConfig = { images: { unoptimized: true }, output: "export" };      // in deployment mode, uncomment this line
const nextConfig = { images: { unoptimized: true } };    // in development mode, uncomment this line

module.exports = nextConfig;

// , output: "export"